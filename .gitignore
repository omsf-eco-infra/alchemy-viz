# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
python/alchemy_viz/_version.py

# Node
node_modules/
ts/dist/
.vite/

# pixi
.pixi/

# notebooks
.ipynb_checkpoints/
# `pixi run marimo` converts the demo notebook into here; the .ipynb is the source
examples/notebooks/.marimo/

# scratch
# Where `pixi run parity-reference` and `pixi run big-network` write. Both run
# to megabytes and neither is an input to anything the repository builds.
scratch/

# editors / OS
.DS_Store
.idea/
.vscode/
